# Plans Directory

This directory is used to store historical implementation plans (`implementation_plan.md`) generated during the planning phase of features or bug fixes.

---

## File Naming Convention
When archiving a plan, copy the approved `implementation_plan.md` into this directory and rename it using the format:
`YYYY-MM-DD-[feature-name]-plan.md`
