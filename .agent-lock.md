# Agent Lock
Current Writer: None
Mode: READ_ONLY
Scope: Bốn lỗi hiển thị Owner chỉ ra ngày 2026-09-25 (ba cái đầu trên ảnh chụp).
       (1) Đầu cột bảng IN ĐƠN vỡ chữ giữa từ ("Nhân viên lấy h|àng"). HAI chẩn đoán đầu đều
           BỊ BÁC BỎ, ghi lại để không ai rà lại:
           - "GridHeaderHeight 30 quá thấp, nâng lên 36": sai, vì ApplyStandardGridSettings
             (Main.cs) ghi đè thẳng ColumnHeadersHeight = 34 nên token không bao giờ tới bảng.
             ThemeMetrics.cs đã revert về 30.
           - "Hai nơi cùng ghi ColumnHeadersDefaultCellStyle.Font (7.5F vs 9F) nên đo một đằng
             vẽ một nẻo": cũng sai. Cắm log vào AutoSizePrintGridColumns chạy trong app thì font
             lúc đo LUÔN là Segoe UI Semibold 9F và bề rộng đã đúng (133/116/104/81/59). Việc xoá
             dòng đặt font thừa trong ApplyStandardGridSettings vẫn giữ (dọn double-writer) nhưng
             KHÔNG phải thứ chữa được lỗi.
           Nguyên nhân THẬT: SetupGrid() gọi DisableSorting() lúc _grid.Columns.Count == 0 (cột
           do AutoGenerateColumns sinh mãi trong RebuildTable() mới ra đời), nên mọi cột giữ
           SortMode mặc định Automatic. DataGridView chừa sẵn ~13px mỗi đầu cột cho mũi tên sắp
           xếp, trong khi phần bù +24 của AutoSizePrintGridColumns chỉ đủ padding 16 + viền 2.
           Thiếu chỗ -> GDI ngắt giữa từ. Dựng harness ngoài app: bản có SortMode = NotSortable
           KHÔNG vỡ, bản để Automatic thì vỡ — đúng một biến khác nhau. Sửa: gọi lại
           DisableSorting() trong RebuildTable(), ĐỨNG TRƯỚC AutoSizePrintGridColumns(). Đây còn
           là vá nghiệp vụ: sắp xếp lẽ ra phải tắt trên bảng này (nên mới có DisableSorting) mà
           thực tế vẫn bật suốt trên mọi cột tự sinh.
       (2) Thẻ "Tóm tắt điều khoản" ở ABOUT cắt mất 2/5 gạch đầu dòng. Label thân Dock.Fill
           nên bị ghim đúng chiều cao ô, chữ tràn bị xén. Hàng 6 của uiTableLayoutPanel22 đã là
           Percent 100 nên thẻ đã ôm hết chỗ dư — mổ thêm hàng Designer không thêm được gì.
           Sửa vòng 1: ACard bật AutoScroll, Label thân đổi sang Dock.Top + AutoSize. Soi bằng
           mắt thì lòi thêm HAI lỗi nữa, đã vá nốt:
           - AutoSize = true MỘT MÌNH tắt ngắt dòng của Label (GetPreferredSize bỏ qua bề rộng
             Dock áp xuống khi MaximumSize.Width = 0) nên chữ nở ngang rồi bị xén cụt đuôi.
             Sửa: SyncSummaryBodyWidth() ghim MaximumSize.Width theo ClientSize của thẻ, gắn vào
             ClientSizeChanged. Phải dùng ClientSize chứ KHÔNG dùng DisplayRectangle: thanh cuộn
             dọc hiện thì chỉ ClientSize hụt đi.
           - Ngắt dòng xong lại vỡ giữa từ ("quy|ền", "vận hà|nh"). Gốc là đường vẽ chữ GDI:
             DrawText ngắt tiếng Việt ngay tại ký tự có dấu khi dòng không vừa. Dựng lại ngoài
             app ở bề rộng 420px: GDI vỡ y hệt, GDI+ ngắt đúng chỗ dấu cách; cờ WordBreak /
             TextBoxControl / NoPrefix không liên quan (thử đủ ba tổ hợp đều vỡ). Sửa:
             UseCompatibleTextRendering = true CHO RIÊNG nhãn thân thẻ.
       (3) Chữ "AutoJMS" hiện hai lần (title bar Windows + TopNavigation) và dính sát tab HOME.
           Owner chọn bỏ ở TopNavigation. Sửa: topNav.ShowIdentity = false (giống
           tabPrint_printTabs và uiTabControl1Strip đã làm).
       (4) Mất nhãn trạng thái mạng "● Online / ● Mạng chậm / ● Mất kết nối". Code chưa hề bị
           xoá — lblNetworkStatus vẫn dựng và vẫn nhận NetworkState.OnChanged, nhưng nó gắn vào
           Form ở y=9. Hồi còn UIForm, thanh tiêu đề do SunnyUI tự vẽ NẰM TRONG vùng client nên
           y=9 rơi đúng vào đó; Form thường có thanh tiêu đề Windows ở NGOÀI vùng client nên
           y=9 rơi xuống dưới topNav (Dock.Top, cao 40) và tabControl — cả hai vào Controls
           trước nên nằm trên, nhãn bị che hẳn. Hai lệnh BringToFront() cũng vô nghĩa vì gọi
           TRƯỚC Controls.Add. Sửa: Parent = topNav, nép mép phải nav lề 12px và căn giữa dọc,
           nghe topNav.SizeChanged thay cho this.SizeChanged. Giữ nguyên bảng màu theo theme.
       File đụng: src/AutoJMS/Forms/Main.cs, src/AutoJMS/Forms/Main.Designer.cs,
       src/AutoJMS/Printing/PrintService.cs.
       Vòng 1 push ở 02ef38f (Release 0/0, verify.ps1 5/5 PASS) nhưng CHƯA soi bằng mắt.
       Vòng 2 (phiên verify-4-ui-fixes): đã soi thật trên bản Debug, cả 4 mục PASS —
       #1 kiểm đủ 4 tab con IN ĐƠN, #2 cuộn hết 5 gạch đầu dòng, #3 nav hết chữ trùng,
       #4 "● Online" nép phải nav. Bẫy đã dính một lần: bản Debug cũ hơn file nguồn làm
       tưởng nhầm là đã sửa xong — LUÔN build lại trước mỗi lần soi.
       Nhả khoá.

Scope (4 phase đã xong, giữ lại để tra cứu): Chuyển toàn bộ UI sang Design System A*, gỡ Sunny.UI (4 phase, Owner duyệt 2026-09-22).
       Phase 1 app shell: XONG (ATabControl + TopNavigation + Main rời UIForm).
       Phase 2 DKCH/HOME: XONG (3 khung -> APanel, 9 nút -> AButton, url bar -> ATextBox,
       thanh công cụ HOME -> APanel, bỏ lớp bọc nút SunnyUI).
       Phase 3a TRACKING: XONG (6 nút -> AButton, ô mã vận đơn -> ATextBox, bảng -> ADataGridView).
       Phase 3b IN ĐƠN: XONG (bảng -> ADataGridView, ô mã vận đơn -> ATextBox, Chọn tất cả -> ACheckBox,
       AutoPrint -> AToggleSwitch, Tìm kiếm/Làm mới -> AButton). Nút IN (UIImageButton, có ảnh) và
       thanh tab con tabPrint_printFunc (UITabControl, dải tab phải HIỆN) để lại cho Phase 4.
       Phase 4 ABOUT + dialog + gỡ SunnyUI: XONG (nút IN -> AButton có Image, tabPrint_printFunc ->
       ATabControl + TopNavigation, ABOUT -> Label/AButton/LinkLabel trong ACard, MessageBox ->
       AMessageDialog/AConfirmDialog, UIMessageTip -> AToast, UIForm -> Form, AppTheme bỏ mọi nhánh
       Sunny, gỡ 2 PackageReference SunnyUI). Không còn tham chiếu Sunny.UI trong src/AutoJMS.
       Toàn bộ 4 phase đã push lên origin/main.
       Rà soát cuối (2026-09-25): 0 code dùng SunnyUI. Dọn nốt phần tài liệu/rule/skill ĐANG SỐNG
       còn dạy agent dùng SunnyUI (rules 01, skill winforms + grid, .agent/README*, project-overview,
       DESIGN.md, docs/architecture|agent|dev|project, 2 prompt template trong eng/prompts) và các
       comment trong code khẳng định sai "AppTheme chỉ nhận ra control SunnyUI".
       Đổi tên nốt .agent/skills/sunnyui-grid-skill.md -> datagridview-grid-skill.md (dấu vết cuối
       cùng của SunnyUI trong repo). Trước đó `git mv` chết vì ACL chứ không phải khoá file: 229/1012
       file tracked thuộc sở hữu BUILTIN\Administrators, chỉ có BUILTIN\Users=(RX,W) nên thiếu quyền
       DELETE. Owner đã chạy icacls cấp Modify cho DAT\DAT trên toàn repo.
       Bước cuối: gỡ tên SunnyUI khỏi 33 khối comment (39 dòng) trong 15 file .cs (tombstone thuần thì xoá,
       comment còn dữ kiện sống thì viết lại bỏ tên). src/ và tests/ nay 0 match. Ba dòng
       "đã gỡ, không thêm lại" ở rules 01, MODULE_BOUNDARIES.md, DesignSystem/README.md GIỮ cố ý.
       XONG — nhả khoá.
       Files: src/AutoJMS/UI/DesignSystem/**, src/AutoJMS/Forms/Main.Designer.cs, src/AutoJMS/Forms/Main.cs,
       src/AutoJMS/Forms/Main.*.cs, src/AutoJMS/UI/AppTheme.cs, DesignReference/**, .agent/**, docs/**.

<!-- Format bắt buộc của `Current Writer`: `<tên agent> (<định danh phiên>)`, ví dụ
     `Claude Code (cleanup-tooling-rules)`. Tên agent trơ trọi là KHÔNG đủ — hai phiên
     Claude Code song song đều tự xưng "Claude Code" nên không phiên nào chặn được phiên nào.
     Xem AGENTS.md § Workspace Lock Rules. -->
