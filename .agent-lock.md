# Agent Lock
Current Writer: Claude Code (dpi-scale-internals)
Mode: WRITE_ACTIVE

Scope: Owner yêu cầu trong chat 2026-09-25 (việc nối tiếp 63de4c4): "these hand-painted internals
       are still raw 96-DPI pixels and do not grow at 125%/150% ... Width measurement helpers
       (DkchDropDown.WidthFor, DkchSpin.WidthFor, ReverseInputBox.MeasureWidth) must add SCALED
       gutters so outer width still fits. At 96 DPI S(x)==x, so 100% must stay pixel-identical".
       File: src/AutoJMS/Forms/Main.DkchData.cs (DkchDropDown + popup, DkchSpin, DkchToggle),
       src/AutoJMS/Forms/Main.DkchNewbill.cs (thẻ Newbill, thanh cuộn hành trình, pill chế độ),
       src/AutoJMS/Forms/Main.TabPrintReverse.cs (ReverseInputBox, ReverseRoundButton,
       _reverseClearStaff). Main.cs (Columns["Select"].Width, RowTemplate.Height) là Protected
       File: CHỈ sửa khi Owner cho phép rõ trong phiên này. KHÔNG đụng Main.Designer.cs,
       Program.cs, font, business logic, API, auth, WebView2 login.

Scope (đã xong ở 63de4c4, giữ để tra cứu): Owner yêu cầu trong chat 2026-09-25: "for each section, wrap hard-coded
       Size/Height/Width/Location/Padding/Margin/RowStyle/ColumnStyle pixel values in
       DpiHelper.Scale / S(), without changing any behavior, handler names, or layout at 100%".
       Các phần: DKCH Data (Main.DkchData.cs), DKCH Newbill (Main.DkchNewbill.cs), khối design
       DKCH (Main.DkchDesignSystem.cs), In Reverse (Main.TabPrintReverse.cs), In Lại Đơn
       (Main.TabPrint.cs), ABOUT summary (Main.cs InitializeAboutSummary — Protected File, CHỈ sửa
       khi Owner cho phép rõ trong phiên này). Không đụng font, business logic, API, auth, WebView2.
       Quyền sửa Main.cs: Owner trả lời câu hỏi trong phiên này "cho phép claude toàn quyền quyết
       định" → sửa InitializeAboutSummary + AlignLeftPanelControls (hàng CONTROL của DKCH, cùng
       loại lỗi số thô ghi đè số đã scale). KHÔNG đụng Main.Designer.cs, Program.cs.

Scope (đã xong ở 0b40f89 + 1e459ca, giữ để tra cứu): Owner yêu cầu trong chat 2026-09-25: "Bỏ viền Form mặc định của hệ điều hành
       (FormBorderStyle.None), xây dựng thanh tiêu đề tùy biến (AppTitleBar) với Form title, nút
       Thu nhỏ / Đóng, và đưa nhãn trạng thái mạng ("● Online") lên thanh tiêu đề ... Ứng dụng bắt
       buộc luôn ở chế độ Windows "Fullsize" nên sẽ không có nút phóng to". Quyền sửa Protected
       File Main.cs/Main.Designer.cs: chính task này (FormBorderStyle nằm trên Main) + chỉ thị
       Phase mở đầu. KHÔNG đụng Program.cs.
       Kèm sửa hai lỗi của chính cf56938: QUY TẮC DPI GHI Ở KHỐI DƯỚI LÀ SAI (xem ghi chú ở đó),
       và hàng thẻ ABOUT hạ 700 -> 420 làm thẻ "Tóm tắt điều khoản" bị bóp mất (commit riêng).

Scope (đã xong ở cf56938, giữ để tra cứu): Owner yêu cầu 2026-09-25: "mở debug và rà soát toàn bộ giao diện hiện tại
       của ứng dụng", chuẩn hoá layout/spacing/alignment/visual hierarchy trên TOÀN BỘ tab theo
       8px grid + Design System A*, và làm app responsive với nhiều độ phân giải. Owner cho phép
       over-running tới khi đạt goal. Nối tiếp b19cc09/a05ac2d.
       Quyền sửa Protected File Main.cs/Main.Designer.cs đến từ chỉ thị Phase mở đầu
       ("Protected files = Main.cs + Main.Designer.cs open").

       [ĐÍNH CHÍNH 2026-09-25, phiên frameless-titlebar] QUY TẮC DƯỚI ĐÂY SAI, ĐỪNG LÀM THEO.
       Đã đo bằng app thử (48-DPI dimensions để hệ số = 2 ở máy 96 DPI): control thêm trong
       InitializeComponent trước ResumeLayout → 80 (được nhân); control thêm trong constructor
       SAU InitializeComponent, lồng trong container của Designer, hay thêm ở OnLoad → đều 40
       (KHÔNG được nhân). Lượt auto-scale chỉ chạy ở ResumeLayout của InitializeComponent.
       Đúng là: control dựng bằng code sau InitializeComponent (kể cả trong constructor) PHẢI
       qua DpiHelper.Scale/S(). Comment cũ trong TopNavigation.cs (đòi S()) mới là đúng.

       QUY TẮC DPI QUAN TRỌNG NHẤT rút ra được, ghi kỹ vì sai là lệch gấp đôi:
       Sau khi Main bật AutoScaleMode.Dpi, control có mặt trong cây TRƯỚC lượt layout đầu tiên
       của Form (dựng trong InitializeComponent hoặc trong constructor Main) được chính WinForms
       nhân lên — nên phải để HẰNG SỐ 96-DPI THÔ. Chỉ control dựng SAU OnLoad mới cần
       DpiHelper.Scale. Trộn hai kiểu là nhân hai lần.
       - BuildTopNavigation() ở Main.cs:170, ngay sau InitializeComponent() → thô.
         Comment cũ trong TopNavigation.cs BẢO đổi sang S(...) là SAI, đã sửa lại comment
         chứ không làm theo. ĐỪNG "sửa" ngược lại.
       - BuildTabPrintInReverseSection() cũng gọi từ constructor → thô.
       - InitializeTabPrintPrinterControls() gọi ở Main.cs:686 trong OnLoad → DpiHelper.Scale,
         đúng và cần thiết.
       Chọn AutoScaleMode.Dpi chứ KHÔNG phải .Font: AppTheme thay Font của mọi control lúc chạy
       nên lấy font làm mốc quy đổi là lấy một mốc di động. Ở 96 DPI hệ số là 1 → no-op trên máy
       chuẩn, chứng minh được trước khi bật.

       GỐC LỖI "lệch đường chân chữ" mà Owner báo: AppTheme.DefaultControlFont là một font RIÊNG
       10F, trong khi control A* dùng token Body 9.75F. Hai control cùng hàng lệch nửa point.
       Sửa bằng cách trỏ DefaultControlFont thẳng vào token Body — một dòng, ở chỗ dùng chung.
       Đã BÁC BỎ cách "map font gần nhất về token trong ApplyStyleToControl": FullStackOperation
       có 60+ font tự tạo sẽ bị đổi dáng im lặng.

       LỖI TÌM ĐƯỢC KHI SOI Ở BỀ NGANG HẸP (không nằm trong danh sách Owner nêu):
       - uiTableLayoutPanel25 chia 50/50 → ở 1366x768 cột trái chỉ còn ~385px, không đủ dải nút
         In Reverse (150+84+34+56+34+132 + margin) nên "Copy mã đã chọn" và bộ chuyển trang bị
         CẮT MẤT. Sửa: Percent 100F + Absolute 210F (đúng bề ngang hai nhãn đếm 115+95).
       - TopNavigation co tỉ lệ khi hẹp làm HorizontalCenter gặm cả hai đầu ("In chuyển hoàn" ra
         "n chuyển hoà", dính liền tab kế). Sửa: thêm TextFormatFlags.EndEllipsis.
       - uiPanel2 cao 35px (client 25px) đang cắt đáy dãy nút 28px của "In chuyển hoàn".

       CỐ Ý KHÔNG ĐỔI: frmLogin.Designer.cs:144 và frmLogin.cs:25 vẫn AutoScaleMode.None —
       form đó là một khung WebView2 phủ kín, WebView2 tự lo DPI, và Owner cấm đụng luồng login.
       Main.DkchData.cs:63 DkchDesignWidth = 270 vẫn đúng: geo_rest.py chỉ hạ padding TRÊN của
       tabDKCH_dataSrc từ 20 xuống 4, giữ nguyên padding trái/phải = 1.

       Đã soi bằng mắt trên bản Debug (build lại trước khi soi), đủ 5 tab và cả 4 tab con của
       IN ĐƠN, ở BA bề ngang: 1920x1080 maximize, 1366x730 (đúng footprint laptop 1366x768) và
       cửa sổ hẹp 960px. Ở 960px dải nút In Reverse vẫn cụt — dưới sàn thiết kế (ClientSize
       1386), không chữa thêm.

       Script sinh sửa đổi hàng loạt nằm ở eng/tmp/geo_print.py và eng/tmp/geo_rest.py, CỐ Ý
       không commit: chạy một lần rồi thôi, chạy lại sẽ không khớp key nữa.

Scope (đã xong ở 7c9390b, giữ để tra cứu): Owner yêu cầu 2026-09-25: "mở debug và test cho anh về giao diện, đồng bộ
       toàn bộ icon theo bộ icon mới". Hai việc: (1) build Debug, chạy app thật,
       soi bằng mắt phần giao diện icon; (2) rà toàn bộ app tìm icon CHƯA theo bộ
       Lucide (ký tự unicode gõ thẳng, emoji, mã MDL2 sót, ảnh dùng làm icon) và
       chuyển sang ASymbols/Lucide. Nối tiếp d95e45d.

       LỖI THẬT TÌM ĐƯỢC KHI SOI BẰNG MẮT — ghi kỹ, đây là loại bẫy sẽ tái diễn:
       MỌI icon Lucide trong app đang ra ô vuông tofu, trong khi build 0 Warning,
       IsEmbeddedFontLoaded == true, và cả 3 test font của d95e45d đều PASS.
       PrivateFontCollection.AddMemoryFont chỉ đăng ký font với GDI+; ASymbols.Draw
       lại vẽ bằng TextRenderer, tức GDI. GDI tra tên family "lucide" trong bảng font
       HỆ THỐNG, không thấy, rồi ÂM THẦM thay bằng font mặc định — font đó không có
       glyph nào trong Private Use Area nên mọi codepoint ra .notdef. Không exception,
       không warning, không log.
       Sửa: gọi thêm AddFontMemResourceEx (gdi32) trên chính buffer đã ghim, ngay cạnh
       AddMemoryFont. Một dòng, ở hàm dùng chung, nên mọi ASymbols.Draw / AButton.Symbol
       được chữa cùng lúc. ĐỪNG "dọn" dòng này.
       Đã loại trừ có hệ thống trước khi kết luận: parse thẳng cmap + post của lucide.ttf
       bằng script -> 31/31 codepoint khớp đúng tên glyph, nên sai mã KHÔNG phải nguyên nhân.

       BÀI HỌC VỀ GATE RỖNG: AllSymbolConstants_AreInPrivateUseArea chỉ kiểm hằng có nằm
       trong DẢI 0xE000–0xF8FF hay không — dải đó đúng kể cả khi font không tới được GDI.
       Test mới Draw_RendersRealGlyph_NotNotdefBox vẽ thật ra Bitmap rồi so pixel với
       0xF8FF (nằm trong PUA nhưng ngoài cmap của lucide.ttf, luôn là .notdef). Đã chứng
       minh nó KHÔNG rỗng: vô hiệu AddFontMemResourceEx -> FAIL đúng thông điệp, rồi khôi
       phục nguyên byte.

       Đã soi bằng mắt trên bản Debug (build lại trước mỗi lần soi), đủ 5 tab:
       HOME (arrow-left/arrow-right/refresh-cw/home/ellipsis), CHUYỂN HOÀN (home/minus/plus,
       nút copy), TRA HÀNH TRÌNH (search/package/refresh-cw/download/external-link/upload),
       IN ĐƠN + tab con In Reverse (calendar/clock/search/X/chevron-left/chevron-right/printer),
       ABOUT (settings) — ABOUT vẫn là tab cuối.

       CHỖ CỐ Ý KHÔNG ĐỔI (Lucide là glyph PUA, đặt vào control dùng font CHỮ thì ra tofu —
       muốn đổi phải viết lại thành custom paint, ngoài phạm vi task này):
       - FullStackOperation.WaybillWorkspace.cs:73 Button Text "← Danh sách đơn"; :176 Label "▲"
       - FullStackOperation.cs:1807/1813 giá trị ô DataGridView "✓"/"✗"
       - FullStackOperation.cs:2585/2587/2592 chip lọc "● {status}", "🔍", "📅"
       - FullStackOperation.cs:3066/3077 thanh tiêu đề Windows "⚠ …" (không gán font riêng được)
       - FullStackOperation.cs:3427 Label delta "▲ "/"▼ "
       - Main.cs:1138/1142/1147 lblNetworkStatus "● Online" (vừa là Protected File vừa là Label chữ)
       - Main.DkchNewbill.cs:922-924, 990/1237/1591; Main.DkchData.cs:480;
         WaybillTrackingService.cs:944
       Loại B — KHÔNG phải icon giao diện, không đụng: chuỗi mô tả của DkchJourneyAnalyzer,
       mọi OnLog?.Invoke trong WebViewAutomation, Tab2Config.ActionPrefix "→ ", bullet của
       TermsContentProvider, ký tự khung trong Program.cs/AppPaths.cs, và "→/↔/⇒" trong comment.

       CHƯA SOI ĐƯỢC (cần Owner smoke test): dấu "▶" của thẻ GỢI Ý và cặp copy->check chỉ
       hiện khi có dữ liệu đơn thật; KpiCard của Design System hiện chưa có nơi nào khởi tạo
       (chỉ KpiCardControl được dùng), nên đổi TrendingUp/TrendingDown không có gì để nhìn.

       Còn trên disk, CỐ Ý không commit: lucide_cmap.json ở gốc repo — bảng codepoint -> tên
       glyph do script parse lucide.ttf sinh ra, dùng để tra mã. Không xoá (CLAUDE.md cấm),
       Owner quyết giữ hay bỏ.

Scope (đã xong ở d95e45d, giữ để tra cứu): Tích hợp Lucide Icons (lucide.ttf font nhúng) và Morphicons cho toàn bộ
       ứng dụng AutoJMS (2026-09-25). Owner yêu cầu trực tiếp trong chat.

       TIẾP QUẢN khoá từ phiên `Claude Code (lucide-morphicons-integration)` — phiên đó
       (tab "AutoJMS UI/UX redesign") bỏ dở, idle 4 tiếng, để lại việc chưa commit. Owner
       xác nhận trong chat ngày 2026-09-25 cho tiếp quản và GIỮ NGUYÊN việc dở trên disk.
       Owner cũng chốt: dùng thư viện Morphicons THẬT (MIT, npm `morphicons`), không giữ
       bản tự viết. Phiên kia không được ghi thêm gì cho tới khi khoá này nhả.

       Đã kiểm (read-only) trước khi nhận khoá:
       - lucide.ttf là font `lucide` thật, 1895 glyph. 31/31 codepoint trong ASymbols.cs
         khớp đúng cmap của chính font đó (đã dò bằng script parse TTF). Phần này ĐÚNG, giữ.
       - morphicons.min.js KHÔNG phải Morphicons: là bản tự viết `AjMorphIcon`, không
         minify, không có spring physics như rule mô tả. Tên file gây hiểu nhầm là code
         bên thứ ba trong repo PUBLIC.
       - WebView2 CHƯA tích hợp gì: index.html nạp 2 script nhưng 0 nơi gọi (AjLucide 0,
         AjMorphIcon 0, data-icon 0).
       - rule 11 §5.B ghi API `new Morphicon(el, {...})` không tồn tại ở đâu cả.

       Phạm vi sửa:
       - src/AutoJMS/UI/DesignSystem/Helpers/ASymbols.cs (thêm PrivateFontCollection + hằng Lucide)
       - src/AutoJMS/AutoJMS.csproj (khai báo EmbeddedResource lucide.ttf)
       - src/AutoJMS/Resources/Fonts/lucide.ttf (NEW — font nhúng)
       - src/AutoJMS/Web/morphicons/** (NEW — morphicons 1.7.1 MIT, chép nguyên 6 file
         theo đúng nhánh phụ thuộc của dist/element.js; có README ghi provenance)
       - src/AutoJMS/Web/aj-icons.js (NEW — điểm vào: dữ liệu Lucide + <aj-morph-icon>)
       - src/AutoJMS/Web/index.html (script type=module, 3 nút copy dùng <aj-morph-icon>)
       - eng/harness/check-dashboard-icons.mjs (NEW) + eng/harness/test-node.ps1 (gọi nó)
       - tests/AutoJMS.Tests/ASymbolsFontTests.cs (NEW — 3 test)
       - src/AutoJMS/UI/DesignSystem/Theme/ThemeMetrics.cs (thêm IconSizeTag/IconSizeHero)
       - DesignReference/AutoJMS.DESIGN.md (cập nhật §J icon policy)
       - .agent/rules/11-icon-and-animation-rules.md (§2 §4 §5 §6 viết lại)

       CHỐT LẠI, để phiên sau khỏi đi lại đường cũ:
       - Giả định "Web/ phải dùng UMD vì không có bundler" là SAI. Dashboard chạy ở origin
         https thật (SetVirtualHostNameToFolderMapping → https://autojms.local), nên
         <script type="module"> + import cùng origin hợp lệ với CSP script-src 'self'.
         Nhờ đó thư viện đi vào repo NGUYÊN BẢN upstream, nâng cấp không phải dựng lại gì.
       - Module script chạy sau khi parse xong document nhưng TRƯỚC DOMContentLoaded, mà
         support.js boot dc-runtime trong DOMContentLoaded → thứ tự luôn đúng. Đừng thêm async.
       - <aj-morph-icon> tồn tại vì thuộc tính spring= chỉ nhận TÊN preset (smooth/snappy/
         bouncy); 200/20 của rule 11 không phải preset nào nên phải gán bằng property.
       - icon= nhận CHUỖI `d`, không nhận tên icon: React 16.8.6 chỉ truyền được thuộc tính
         dạng chuỗi cho custom element. canonicalD() gộp rect/circle/path về một `d`.
       - Font: 31/31 codepoint khớp cmap của lucide.ttf. AddMemoryFont KHÔNG copy vùng nhớ —
         bản cũ free GCHandle trong finally và để fontData là biến cục bộ, tức là GC được
         phép dọn/di chuyển vùng nhớ mà font đang trỏ vào. Đã chuyển sang field static, ghim
         vĩnh viễn. Đừng "dọn" lại chỗ này.
       - lucide-icons.js và morphicons.min.js của phiên trước đã đổi tên thành *.bak (CLAUDE.md
         cấm xoá; .bak vừa bị .gitignore vừa bị loại khỏi glob Content của csproj).
         morphicons.min.js KHÔNG phải Morphicons — đừng khôi phục.
       - CHƯA SOI BẰNG MẮT phần Dashboard: browser pane không mở được file:// của trang này
         (thành data:, styles.css/app.js không nạp). Cần Owner smoke test 3 nút copy.

Scope (đã xong ở b19cc09, giữ để tra cứu): topNav ra khỏi Designer + rules/prompt Lucide. Owner yêu cầu lần thứ hai
       trong chat: "xử lý vấn đề tồn tại cả topNav và tabControl trước đó" — lần trước chỉ giải
       thích là cố ý, lần này Owner muốn SỬA. Quyền sửa Protected File Main.cs/Main.Designer.cs
       đến từ chỉ thị Phase mở đầu ("App shell = Replace with real TopNavigation", "Protected
       files = Main.cs + Main.Designer.cs open") — đây đúng là phần app shell đó.
       ĐÃ KIỂM: lúc CHẠY chỉ có MỘT thanh. Bật bản Debug (PID 21788), chụp + zoom vùng đỉnh:
       title bar -> topNav (HOME/CHUYỂN HOÀN/TRA HÀNH TRÌNH/IN ĐƠN/ABOUT + "● Online") -> thẳng
       xuống thanh công cụ WebView. Không có dải tab comctl32. Ảnh Owner gửi thiếu hẳn nhãn
       "● Online" — nhãn đó do InitNetworkUI() dựng trong OnLoad, mà OnLoad KHÔNG chạy ở Designer,
       nên ảnh đó chắc chắn là màn Designer của Visual Studio.
       SỬA: đưa topNav RA KHỎI Main.Designer.cs, dựng bằng code trong constructor Main.cs ngay sau
       InitializeComponent(). Designer không biết control dựng lúc chạy nên chỉ còn vẽ dải tab gốc
       — vẫn kéo-thả sửa được 5 TabPage, đúng lý do dải tab được giữ hiện. Lúc chạy y hệt cũ.
       KHÔNG chọn cách bỏ "&& !DesignMode" trong ATabControl: ẩn dải tab gốc trên Designer thì
       không còn chỗ bấm đổi trang, phải đi vòng qua Document Outline — mất workflow của Owner.
       Đây cũng là cách FullStackOperation.BuildUiInCode đã làm sẵn cho cặp uiTabControl1 /
       uiTabControl1Strip, nên không đẻ ra pattern mới.
       Lợi phụ: chốt luôn cái bẫy CS1061 đã cảnh báo phiên trước. topNav không còn được Designer
       serialize, nên VS có cache design-time assembly cũ cũng không thể ghi lại
       "topNav.ProductTitle = ..." vào Main.Designer.cs khi Owner bấm Save.
       KHÔNG đụng: TopNavigation.cs, ATabControl.cs, thứ tự tab, Target, dock, màu, handler nào.

Scope (đã xong ở 288fb46, giữ để tra cứu): Nhãn trạng thái mạng vô hình ở theme Red (2026-09-25). Owner yêu cầu rõ trong chat
       "sửa luôn màu trắng ở theme Red đi" — đây là quyền sửa Protected File
       src/AutoJMS/Forms/Main.cs cho ĐÚNG việc này, nối tiếp c90b699.
       Phát hiện khi chạy thử: theme Red đặt nhãn thành Color.White (Online/Mạng chậm) và
       Color.Black (Mất kết nối), nhưng TopNavigation.OnPaint tô nền thanh nav bằng
       c.Surface, mà ThemeColors.Red.Surface = "#F5F7FA" — GIỐNG HỆT ThemeColors.Light.
       Light và Red chỉ khác nhau ở bộ Primary* (xanh #3B82F6 vs đỏ #E53935); Surface,
       SurfaceAlt, SurfaceRaised, Border, Text đều trùng từng byte. Nên chữ trắng nằm trên
       nền gần trắng: tỉ lệ tương phản ~1.06:1, thực tế là vô hình.
       Bảng màu đỏ này là di sản thời SunnyUI khi thanh header CÓ nền đỏ thật. Sau khi
       chuyển sang Design System A* thì nền hết đỏ, màu chữ không ai chỉnh theo.
       SỬA: bỏ hẳn nhánh isRed trong UpdateNetworkUI. Red dùng đúng màu của Light — không
       phải đặt màu mới, vì hai theme có cùng nền thì phải cùng màu chữ. Nhánh isRed vốn đã
       thừa, giữ lại là giữ một bảng màu thứ hai cho cùng một nền.
       KHÔNG đụng màu của Light và Dark — Owner đã chốt và chúng đang đúng nền của mình.
       Nhả khoá: Release 0 Error 0 Warning, verify.ps1 5/5 PASS, đã chạy thử bản Debug và
       chụp lại nhãn ở theme Red (xanh lá, đọc rõ). Commit 288fb46 đã push lên origin/main.
       Đổi theme lúc test KHÔNG ghi gì lên tài khoản JMS: WebView đứng ở màn hình đăng nhập
       nên SyncJmsWebViewThemeAsync dừng ở nhánh "sysUser id not found"; grep -c updateConfig
       trong debug.log = 0.

Scope (đã xong ở c90b699, giữ để tra cứu): Nhãn trạng thái mạng lblNetworkStatus không đổi màu theo theme (2026-09-25).
       Owner duyệt trong chat cho phép sửa Protected File src/AutoJMS/Forms/Main.cs cho ĐÚNG
       task này, và giao toàn quyền quyết định hướng xử lý phần font.
       MỘT CHI TIẾT TRONG PROMPT ĐỀ XUẤT SAI, đã kiểm lại, ghi ra để không ai chép lại:
       "mở app với theme Dark/Red đã lưu thì nhãn nhận màu nhánh Light" là SAI. Constructor
       đã đặt UI.AppTheme.CurrentTheme từ _settings.Theme ở Main.cs:211, TRƯỚC khi OnLoad gọi
       InitNetworkUI() ở Main.cs:576. Khối 588-591 chỉ gán lại đúng giá trị cũ. Đường khởi
       động LÀNH — chỉ còn MỘT đường hỏng thật.
       Gốc lỗi (đường duy nhất): handler dropdown Theme ở tab ABOUT (Main.cs:1239-1254) gọi
       UI.AppTheme.Apply(this) nhưng AppTheme.cs:255 CỐ Ý bỏ qua lblNetworkStatus
       ("Ignore, managed by Main.cs"), mà Main.cs lại không tô lại. UpdateNetworkUI là chỗ
       DUY NHẤT đặt ForeColor và nó đọc CurrentTheme ngay lúc gọi, nên nhãn giữ màu theme CŨ
       tới khi NetworkState.OnChanged tình cờ nổ.
       Sửa 1: thêm UpdateNetworkUI(_currentNetworkStatus) ngay sau AppTheme.Apply(this) trong
       handler đó. Đúng một dòng. Không cần guard null: InitNetworkUI chạy ở OnLoad:576 còn
       cboTheme mới dựng ở InitializeAboutSummary() (OnLoad:585), handler chỉ nổ khi người
       dùng bấm. Dòng này cũng biến _currentNetworkStatus từ field GHI-MÀ-KHÔNG-ĐỌC thành
       field có người đọc — đúng chỗ đáng đọc.
       Sửa 2 (font, Owner giao tự quyết): lblNetworkStatus.Font đang là font TỰ TẠO
       new Font("Segoe UI", 9.75F, Bold) nên ThemeTypography.IsToken (so bằng ReferenceEquals)
       trả false, và AppTheme.cs:230 kéo nó về DefaultControlFont 10F Regular ở MỌI lần Apply
       — kể cả lần Apply lúc khởi động. Nghĩa là nhãn CHƯA BAO GIỜ đậm như code tưởng.
       Chọn cách gốc: đổi sang token ThemeTypography.BodyStrong (Segoe UI Semibold 9.75F, nằm
       trong bảng Tokens) thay vì tự đặt lại font sau mỗi Apply. Token thì AppTheme tự bỏ qua
       vĩnh viễn, không phải tô lại, và không rò GDI handle như việc new Font trong
       UpdateNetworkUI (hàm này nổ theo mỗi lần đổi trạng thái mạng).
       KHÔNG đổi bảng màu 3 trạng thái x 3 theme — Owner đã chốt từng màu.
       KHÔNG viết unit test: CLAUDE.md nói rõ TDD chỉ áp cho lớp logic thuần, WinForms verify
       bằng Owner Manual Test Checklist.
       Nhả khoá: Release build 0 Error 0 Warning, verify.ps1 5/5 PASS, commit c90b699 đã push
       lên origin/main. Chờ Owner smoke test dropdown Theme ở tab ABOUT (không tự bấm vì
       handler gọi SyncJmsWebViewThemeAsync ghi cấu hình lên tài khoản JMS thật).

Scope (đã xong, giữ để tra cứu): Owner hỏi ngày 2026-09-25: "design của Main.cs vẫn đang chứa cả 2 topNav và tabControl",
       yêu cầu bảo đảm app sạch, không trùng thành phần, và cho phép gỡ phần thừa miễn không
       phá giao diện/logic.
       KẾT LUẬN: topNav + tabControl KHÔNG trùng nhau, là MỘT widget tách đôi cố ý.
       ATabControl.WndProc trả 1 cho TCM_ADJUSTRECT (0x1328) CHỈ KHI !DesignMode, nên lúc chạy
       trang phủ kín dải tab comctl32 (một thanh), còn trên mặt Designer của Visual Studio dải
       tab gốc vẫn hiện để Owner kéo-thả sửa được 5 TabPage (hai thanh). Đã chụp màn hình VS
       xác nhận đúng hiện tượng này. TopNavigation không giữ SelectedIndex — Target.SelectedIndex
       là nguồn sự thật duy nhất, nên mọi handler tabControl_SelectedIndexChanged chạy y như cũ.
       Xoá một trong hai là mất tab hoặc mất thanh điều hướng. KHÔNG sửa cặp này.
       Đã soát máy móc Main.Designer.cs bằng script đi ngược chuỗi Parent: 91 control được tạo,
       0 control mồ côi, duy nhất tabControl và topNav nằm ở mức Form. Không có trùng lặp.
       Phần THỪA THẬT đã gỡ trong task này:
       - TopNavigation.ShowIdentity/ProductTitle/DrawIdentity/IdentityGap: nhánh vẽ icon + chữ
         "AutoJMS" KHÔNG BAO GIỜ chạy vì cả 3 chỗ dựng TopNavigation đều đặt ShowIdentity=false
         (Main.Designer.cs:589 và :1482, FullStackOperation.Layout.cs:58). Owner đã chốt bỏ ở
         phiên trước (lý do: thanh tiêu đề Windows đã mang sẵn icon + tên sản phẩm). Gỡ luôn
         property để không còn công tắc cho một nhánh đã bị loại.
       - AppPaths.SecureTempFile (AppPaths.cs:61): khai báo, 0 nơi dùng trong toàn repo.
       KHÔNG đụng (báo cáo cho Owner quyết, vì CLAUDE.md cấm agent tự xoá file):
       - 10 file UI/DesignSystem chưa ai dùng + src/AutoJMS/UI/AppPalette.cs.
       - 6 hàm CreateThoiHieu* chết trong FullStackOperation.cs.
       - Main.cs:155 _currentNetworkStatus GHI MÀ KHÔNG ĐỌC: đây KHÔNG phải code chết để xoá mà
         là dây nối chưa đấu. AppTheme.cs:255 cố ý bỏ qua lblNetworkStatus ("managed by Main.cs")
         còn Main.cs không hề tô lại nhãn sau AppTheme.Apply(this), nên đổi theme (ABOUT) hoặc mở
         app với theme Dark/Red đã lưu thì nhãn giữ màu của theme CŨ tới khi mạng đổi trạng thái.
         Xoá field là chôn luôn manh mối. Để lại, báo Owner thành task riêng.
       Đã push a4f7ba1 (Release 0 Warning/0 Error, verify.ps1 5/5 PASS). Nhả khoá.

Scope (đã xong, giữ để tra cứu): Bug data-loss license.dat ngày 2026-09-25 (Antigravity Prompt Proposal, Owner duyệt
       trong chat cho phép sửa Protected File src/AutoJMS/Program.cs cho ĐÚNG task này).
       Triệu chứng: app mở lên thẳng dialog kích hoạt dù license còn hạn; license.dat đúng
       373 byte mà cả 373 đều là 0x00, hai file cùng thư mục vẫn lành. HWID không đổi.
       Gốc: Program.cs:450 File.WriteAllText ghi đè thẳng file đích. NTFS ghi kích thước vào
       MFT ngay còn nội dung nằm trong page cache, tắt cứng ngay sau đó thì file dựng lại
       ĐÚNG kích thước mà toàn byte 0. ReadLocalCache catch rỗng nuốt JsonException nên
       không để lại dấu vết nào trong debug.log.
       HAI chi tiết trong prompt đề xuất SAI, đã kiểm lại, ghi ra để không ai chép lại:
       - AppPaths.SecureTempFile là CODE CHẾT (khai ở AppPaths.cs:61, không ai dùng). Thứ
         thật sự cứu AutoJMS.secure là AppConfig.AtomicWriteAllText, bản private tự ghép .tmp.
       - Temp-rồi-move KHÔNG đủ crash-safe. Repo có ~8 bản chép tay đều dùng
         File.WriteAllText(temp) nên dữ liệu .tmp CŨNG nằm trong page cache và cũng zero
         được; thứ cứu được chỉ là file đích không bị đụng. Muốn an toàn thật phải
         Flush(flushToDisk: true) trước khi đổi chỗ.
       Sửa: thêm AppPaths.AtomicWriteAllText (temp + Flush(true) + File.Replace, rơi về Move
       khi chưa có file đích) và cho SaveLocalCache gọi nó — Protected File chỉ đổi đúng một
       dòng. Thêm AppLogger.Warning vào catch của SaveLocalCache và ReadLocalCache.
       KHÔNG gộp 8 bản chép tay còn lại: ngoài phạm vi, để lại đề xuất cho Owner.
       BẪY line-ending đã dính khi commit, ghi lại để phiên sau khỏi mất thời gian: blob
       AppPaths.cs trong HEAD TRỘN lẫn (127 dòng CRLF + 19 dòng LF trần), mà `git add` trên
       máy này KHÔNG normalize dù core.autocrlf=true. Sửa file bằng tool Edit là nó ghi lại
       toàn bộ theo CRLF, nên staged diff phình lên 54/19 với 19 dòng "đổi" mà mắt nhìn y
       hệt nhau. Đã ghép lại ở mức byte (giữ nguyên bytes HEAD cho 146 dòng cũ, chỉ chèn 35
       dòng mới) để diff còn đúng 35/0. Đừng tin `grep -c $'\r'` — grep của Git Bash mở file
       ở text mode nên nuốt CR và báo 0 sai; đếm bằng Python đọc 'rb'.
       Đã push b9768b6 (Release 0 Warning/0 Error, verify.ps1 5/5 PASS, 4 test mới PASS).
       Nhả khoá.

Scope (đã xong, giữ để tra cứu): Bốn lỗi hiển thị Owner chỉ ra ngày 2026-09-25 (ba cái đầu trên ảnh chụp).
       (1) Đầu cột bảng IN ĐƠN vỡ chữ giữa từ ("Nhân viên lấy h|àng"). HAI chẩn đoán đầu đều
           BỊ BÁC BỎ, ghi lại để không ai rà lại:
           - "GridHeaderHeight 30 quá thấp, nâng lên 36": sai, vì ApplyStandardGridSettings
             (Main.cs) ghi đè thẳng ColumnHeadersHeight = 34 nên token không bao giờ tới bảng.
             ThemeMetrics.cs đã revert về 30.
           - "Hai nơi cùng ghi ColumnHeadersDefaultCellStyle.Font (7.5F vs 9F) nên đo một đằng
             vẽ một nẻo": cũng sai. Cắm log vào AutoSizePrintGridColumns chạy trong app thì font
             lúc đo LUÔN là Segoe UI Semibold 9F và bề rộng đã đúng (133/116/104/81/59). Việc xoá
             dòng đặt font thừa trong ApplyStandardGridSettings vẫn giữ (dọn double-writer) nhưng
             KHÔNG phải thứ chữa được lỗi.
           Nguyên nhân THẬT: SetupGrid() gọi DisableSorting() lúc _grid.Columns.Count == 0 (cột
           do AutoGenerateColumns sinh mãi trong RebuildTable() mới ra đời), nên mọi cột giữ
           SortMode mặc định Automatic. DataGridView chừa sẵn ~13px mỗi đầu cột cho mũi tên sắp
           xếp, trong khi phần bù +24 của AutoSizePrintGridColumns chỉ đủ padding 16 + viền 2.
           Thiếu chỗ -> GDI ngắt giữa từ. Dựng harness ngoài app: bản có SortMode = NotSortable
           KHÔNG vỡ, bản để Automatic thì vỡ — đúng một biến khác nhau. Sửa: gọi lại
           DisableSorting() trong RebuildTable(), ĐỨNG TRƯỚC AutoSizePrintGridColumns(). Đây còn
           là vá nghiệp vụ: sắp xếp lẽ ra phải tắt trên bảng này (nên mới có DisableSorting) mà
           thực tế vẫn bật suốt trên mọi cột tự sinh.
       (2) Thẻ "Tóm tắt điều khoản" ở ABOUT cắt mất 2/5 gạch đầu dòng. Label thân Dock.Fill
           nên bị ghim đúng chiều cao ô, chữ tràn bị xén. Hàng 6 của uiTableLayoutPanel22 đã là
           Percent 100 nên thẻ đã ôm hết chỗ dư — mổ thêm hàng Designer không thêm được gì.
           Sửa vòng 1: ACard bật AutoScroll, Label thân đổi sang Dock.Top + AutoSize. Soi bằng
           mắt thì lòi thêm HAI lỗi nữa, đã vá nốt:
           - AutoSize = true MỘT MÌNH tắt ngắt dòng của Label (GetPreferredSize bỏ qua bề rộng
             Dock áp xuống khi MaximumSize.Width = 0) nên chữ nở ngang rồi bị xén cụt đuôi.
             Sửa: SyncSummaryBodyWidth() ghim MaximumSize.Width theo ClientSize của thẻ, gắn vào
             ClientSizeChanged. Phải dùng ClientSize chứ KHÔNG dùng DisplayRectangle: thanh cuộn
             dọc hiện thì chỉ ClientSize hụt đi.
           - Ngắt dòng xong lại vỡ giữa từ ("quy|ền", "vận hà|nh"). Gốc là đường vẽ chữ GDI:
             DrawText ngắt tiếng Việt ngay tại ký tự có dấu khi dòng không vừa. Dựng lại ngoài
             app ở bề rộng 420px: GDI vỡ y hệt, GDI+ ngắt đúng chỗ dấu cách; cờ WordBreak /
             TextBoxControl / NoPrefix không liên quan (thử đủ ba tổ hợp đều vỡ). Sửa:
             UseCompatibleTextRendering = true CHO RIÊNG nhãn thân thẻ.
       (3) Chữ "AutoJMS" hiện hai lần (title bar Windows + TopNavigation) và dính sát tab HOME.
           Owner chọn bỏ ở TopNavigation. Sửa: topNav.ShowIdentity = false (giống
           tabPrint_printTabs và uiTabControl1Strip đã làm).
       (4) Mất nhãn trạng thái mạng "● Online / ● Mạng chậm / ● Mất kết nối". Code chưa hề bị
           xoá — lblNetworkStatus vẫn dựng và vẫn nhận NetworkState.OnChanged, nhưng nó gắn vào
           Form ở y=9. Hồi còn UIForm, thanh tiêu đề do SunnyUI tự vẽ NẰM TRONG vùng client nên
           y=9 rơi đúng vào đó; Form thường có thanh tiêu đề Windows ở NGOÀI vùng client nên
           y=9 rơi xuống dưới topNav (Dock.Top, cao 40) và tabControl — cả hai vào Controls
           trước nên nằm trên, nhãn bị che hẳn. Hai lệnh BringToFront() cũng vô nghĩa vì gọi
           TRƯỚC Controls.Add. Sửa: Parent = topNav, nép mép phải nav lề 12px và căn giữa dọc,
           nghe topNav.SizeChanged thay cho this.SizeChanged. Giữ nguyên bảng màu theo theme.
       File đụng: src/AutoJMS/Forms/Main.cs, src/AutoJMS/Forms/Main.Designer.cs,
       src/AutoJMS/Printing/PrintService.cs.
       Vòng 1 push ở 02ef38f (Release 0/0, verify.ps1 5/5 PASS) nhưng CHƯA soi bằng mắt.
       Vòng 2 (phiên verify-4-ui-fixes): đã soi thật trên bản Debug, cả 4 mục PASS —
       #1 kiểm đủ 4 tab con IN ĐƠN, #2 cuộn hết 5 gạch đầu dòng, #3 nav hết chữ trùng,
       #4 "● Online" nép phải nav. Bẫy đã dính một lần: bản Debug cũ hơn file nguồn làm
       tưởng nhầm là đã sửa xong — LUÔN build lại trước mỗi lần soi.
       Nhả khoá.

Scope (4 phase đã xong, giữ lại để tra cứu): Chuyển toàn bộ UI sang Design System A*, gỡ Sunny.UI (4 phase, Owner duyệt 2026-09-22).
       Phase 1 app shell: XONG (ATabControl + TopNavigation + Main rời UIForm).
       Phase 2 DKCH/HOME: XONG (3 khung -> APanel, 9 nút -> AButton, url bar -> ATextBox,
       thanh công cụ HOME -> APanel, bỏ lớp bọc nút SunnyUI).
       Phase 3a TRACKING: XONG (6 nút -> AButton, ô mã vận đơn -> ATextBox, bảng -> ADataGridView).
       Phase 3b IN ĐƠN: XONG (bảng -> ADataGridView, ô mã vận đơn -> ATextBox, Chọn tất cả -> ACheckBox,
       AutoPrint -> AToggleSwitch, Tìm kiếm/Làm mới -> AButton). Nút IN (UIImageButton, có ảnh) và
       thanh tab con tabPrint_printFunc (UITabControl, dải tab phải HIỆN) để lại cho Phase 4.
       Phase 4 ABOUT + dialog + gỡ SunnyUI: XONG (nút IN -> AButton có Image, tabPrint_printFunc ->
       ATabControl + TopNavigation, ABOUT -> Label/AButton/LinkLabel trong ACard, MessageBox ->
       AMessageDialog/AConfirmDialog, UIMessageTip -> AToast, UIForm -> Form, AppTheme bỏ mọi nhánh
       Sunny, gỡ 2 PackageReference SunnyUI). Không còn tham chiếu Sunny.UI trong src/AutoJMS.
       Toàn bộ 4 phase đã push lên origin/main.
       Rà soát cuối (2026-09-25): 0 code dùng SunnyUI. Dọn nốt phần tài liệu/rule/skill ĐANG SỐNG
       còn dạy agent dùng SunnyUI (rules 01, skill winforms + grid, .agent/README*, project-overview,
       DESIGN.md, docs/architecture|agent|dev|project, 2 prompt template trong eng/prompts) và các
       comment trong code khẳng định sai "AppTheme chỉ nhận ra control SunnyUI".
       Đổi tên nốt .agent/skills/sunnyui-grid-skill.md -> datagridview-grid-skill.md (dấu vết cuối
       cùng của SunnyUI trong repo). Trước đó `git mv` chết vì ACL chứ không phải khoá file: 229/1012
       file tracked thuộc sở hữu BUILTIN\Administrators, chỉ có BUILTIN\Users=(RX,W) nên thiếu quyền
       DELETE. Owner đã chạy icacls cấp Modify cho DAT\DAT trên toàn repo.
       Bước cuối: gỡ tên SunnyUI khỏi 33 khối comment (39 dòng) trong 15 file .cs (tombstone thuần thì xoá,
       comment còn dữ kiện sống thì viết lại bỏ tên). src/ và tests/ nay 0 match. Ba dòng
       "đã gỡ, không thêm lại" ở rules 01, MODULE_BOUNDARIES.md, DesignSystem/README.md GIỮ cố ý.
       XONG — nhả khoá.
       Files: src/AutoJMS/UI/DesignSystem/**, src/AutoJMS/Forms/Main.Designer.cs, src/AutoJMS/Forms/Main.cs,
       src/AutoJMS/Forms/Main.*.cs, src/AutoJMS/UI/AppTheme.cs, DesignReference/**, .agent/**, docs/**.

<!-- Format bắt buộc của `Current Writer`: `<tên agent> (<định danh phiên>)`, ví dụ
     `Claude Code (cleanup-tooling-rules)`. Tên agent trơ trọi là KHÔNG đủ — hai phiên
     Claude Code song song đều tự xưng "Claude Code" nên không phiên nào chặn được phiên nào.
     Xem AGENTS.md § Workspace Lock Rules. -->
